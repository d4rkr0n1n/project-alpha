name: Java CI with Maven

on:
  pull_request:
    branches: [ "actions-test" ]
permissions: 
  contents: write
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run: gh --version
